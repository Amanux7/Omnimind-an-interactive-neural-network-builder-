
  # Interactive neural network builder

  This is a code bundle for Interactive neural network builder. The original project is available at https://www.figma.com/design/mnFaley2GgzvnYzITWad4P/Interactive-neural-network-builder.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  